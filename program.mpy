def foo(y):
    f = lambda x: x + y
    u = lambda z: z + y
    return f(1) + u(2)
#end

a = foo(5)
print(a)

l = [1, 2, 3, 4, 5]
print(l.map(lambda x: x + 1))


def f(x):
    if (x > 0):
        print(x)
        f(x-1)
    else:
        print("done")
    #end
#end

f(3)
